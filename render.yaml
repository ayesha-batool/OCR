services:
  - type: web
    name: ocr-extractor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn process_images:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
